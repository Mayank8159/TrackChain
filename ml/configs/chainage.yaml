# Chainage bin size and sensor-to-distance mapping.

step_size_m: 0.25
window_length_m: 25.0
overlap_ratio: 0.20

sensors:
  optical_camera:
    fps: 30
    spatial_resolution_mm: 1.0
  imu_accelerometer:
    sample_rate_hz: 500
  laser_profiler:
    sample_rate_hz: 200
