# Bi-LSTM window size, features, and hyperparameters.

window_length_m: 25.0
sequence_length: 100
input_dim: 6
hidden_dim: 64
num_layers: 2
dropout: 0.2
learning_rate: 0.001
epochs: 30
batch_size: 32

classes:
  0: nominal
  1: gauge_widening
  2: twist_exceedance
  3: corrugation
