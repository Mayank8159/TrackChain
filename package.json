{
  "name": "trackchain-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo root. Defines JS/TS workspaces (app, packages/shared) and root scripts.",
  "_comment": "Monorepo root. Defines JS/TS workspaces (app, packages/shared) and root scripts.",
  "packageManager": "pnpm@11.17.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "test": "turbo test",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md}\""
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "turbo": "^2.3.3"
  }
}
