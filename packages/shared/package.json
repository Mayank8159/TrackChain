{
  "name": "@trackchain/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript package consumed by app and any Node tooling.",
  "_comment": "Shared TypeScript package consumed by app and any Node tooling.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
